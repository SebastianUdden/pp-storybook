import { Meta, Story } from "@storybook/addon-docs/blocks";
import ColoredBox from "./ColoredBox";

<Meta title="Design system|Colored box" component={ColoredBox} />

# Colored box

With `MDX` we can define a story for `Colored box` right in the middle of our markdown documentation.

## Default

<Story name="default">
  <ColoredBox backgroundColor="#666666" />
</Story>
