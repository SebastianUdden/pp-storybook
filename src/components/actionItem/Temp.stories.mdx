import { Meta, Story } from "@storybook/addon-docs/blocks";
import ActionItem from "./ActionItem";

<Meta title="Design system|ActionItem" component={ActionItem} />

# ActionItem

With `MDX` we can define a story for `ActionItem` right in the middle of our markdown documentation.

## Default

<Story name="default">
  <ActionItem />
</Story>
