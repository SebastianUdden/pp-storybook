import { Meta, Story } from "@storybook/addon-docs/blocks";
import MarkdownParser from "./MarkdownParser";
import MarkdownParserWrapper, { MarkdownWrapper } from "./utils";

<Meta title="Design system|Markdown parser" component={MarkdownParser} />

# Markdown parser

With `MDX` we can define a story for `Markdown parser` right in the middle of our markdown documentation.

## Default

<Story name="default">
  <MarkdownParserWrapper />
</Story>

## Restyled

<Story name="restyled">
  <MarkdownParserWrapper MarkdownWrapper={MarkdownWrapper} />
</Story>
