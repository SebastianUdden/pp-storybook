import { Meta, Story } from "@storybook/addon-docs/blocks";

import { MAIN_THEME } from "../../constants/theme";
import { FlexWrapper } from "./utils";
import ContainedButton from "../button/ContainedButton";
import TextButton from "../button/TextButton";
import FabButton from "../button/FabButton";
import OutlineButton from "../button/OutlineButton";
import Ripple from "./Ripple";

<Meta title="Design system|Ripple" component={Ripple} />

# Ripple

With `MDX` we can define a story for `Ripple` right in the middle of our markdown documentation.

## Default

<Story name="default">
  <FlexWrapper>
    <ContainedButton onClick={() => console.log("Click button")}>
      Ripple
    </ContainedButton>
    <OutlineButton onClick={() => console.log("Click button")}>
      Ripple
    </OutlineButton>
    <TextButton onClick={() => console.log("Click button")}>Ripple</TextButton>
    <FabButton onClick={() => console.log("Click button")}>+</FabButton>
  </FlexWrapper>
</Story>
